namespace TinyUpdate.TUUP;

public class TuupUpdatePackageCreatorOptions
{
    /// <summary>
    /// If the update package created should be fully compatible with V1 of Tuup format
    /// </summary>
    public bool V1Compatible { get; }
}